import { Stack } from "expo-router";
import { Text } from "react-native-ui-lib";

export default function Layout() {
    return (
        <Stack screenOptions={{
            headerShown:false
        }}/>
    );
  }
  