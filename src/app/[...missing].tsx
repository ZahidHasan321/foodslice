import React from "react"
import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native-ui-lib"

const errorPage = () => {
    return(
        <SafeAreaView>
            <Text text100>Error 404</Text>
        </SafeAreaView>
    )
}